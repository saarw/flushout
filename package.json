{
  "name": "flushout",
  "version": "1.0.0",
  "description": "Event-sourcing based data model where clients update local proxy models that flush updates to a remote master model that reconciles all changes.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "lint": "tslint -p tsconfig.json"
  },
  "author": "William Saar",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "jest": "^24.8.0",
    "prettier": "^1.18.2",
    "ts-jest": "^24.0.2",
    "tslint": "^5.18.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.5.2"
  }
}
